def sum_array(arr):
    sum = 0
    for i in range(len(arr)):
        sum += arr[i]
    return sum


def product_array(arr):
    sum = 1
    for i in range(len(arr)):
        sum *= arr[i]
        return sum


